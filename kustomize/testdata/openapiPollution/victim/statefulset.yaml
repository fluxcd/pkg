apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: test-statefulset
spec:
  replicas: 1
  selector:
    matchLabels:
      app: stateful
  serviceName: stateful-svc
  template:
    spec:
      containers:
      - name: busybox
        image: busybox:latest
